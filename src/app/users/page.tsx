import { Metadata } from 'next';

export const metadata: Metadata = {
  title: {
    template: 'Página dos usuários',
  },
};

export default function UsersPage() {
  return <>Essa é minha página de users</>;
}
